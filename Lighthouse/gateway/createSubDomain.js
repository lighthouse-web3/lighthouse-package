const axios = require("axios");

const lighthouseConfig = require("../../lighthouse.config");

module.exports = async () => {
  try {
    // Create subdomain
  } catch (error) {
    return null;
  }
};
